int A, dabatase, altura;//no se puede usar la palabra "base" por ello uso "dabatase" que es lo mismo

int número;
int número = console.readline();
if (número > 0)
{
    console.writeline("Es un número positivo");
}
else
{
    Console.WriteLine("Es un número negativo.");
}
Console.ReadKey;

